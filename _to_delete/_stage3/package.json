{
  "name": "refrigeration-cycle-simulator",
  "version": "0.8.0",
  "private": true,
  "description": "Interactive refrigeration training tool: cycle simulator, gauge manifold, fault diagnosis quiz and PT trainer. Static site, no dependencies.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build:scorm": "node tools/build-scorm.js",
    "build:index": "node tools/build-course-index.js"
  }
}
